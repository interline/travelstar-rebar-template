{erl_opts, [
    warnings_as_errors,
    {parse_transform, lager_transform}
]}.

{eunit_opts, [verbose]}.
{cover_enabled, true}.

{deps, [
    {lager, ".*", {git, "git://github.com/basho/lager.git"}}
]}.

{sub_dirs, ["rel"]}.
