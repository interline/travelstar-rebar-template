{deps, [
    {lager, ".*", {git, "git@github.com:devinus/lager.git", {branch, "truncation-size"}}}
]}.
{erl_opts, [
    warnings_as_errors,
    {parse_transform, lager_transform},
    {lager_truncation_size, 10485760}
]}.
{eunit_opts, [verbose]}.
{cover_enabled, true}.
{sub_dirs, ["rel"]}.
